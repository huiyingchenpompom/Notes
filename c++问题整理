1.介绍下C++程序从编写到可执行的整个过程?
-编写代码：.cpp .h
-预处理：将#include替换为头文件内容、处理宏#define将代码替换为宏  #ifdef #ifndef决定是否编译部分代码、清除注释
-编译：主要是将预处理后的代码转化为汇编代码
-汇编: 将汇编代码转化为机器码，生成目标文件 linux环境.o文件windows环境生成obj文件
-链接：生成可执行文件.exe  ，库文件(.dll/.o  .lib/.a)


2.dll和lib的区别？
  -lib是编译时需要的库文件，dll是运行时需要的库文件
  -lib：静态库lib：链接时完全合并到可执行文件中，但是生成的exe文件更大，但不需要外部依赖
        导入库lib:与dll配套使用，包含dll中的符号信息，链接器使用它解析dll中的函数引用，开发编译的时候是需要的，程序打包中不需要
  -dll：
      可执行代码，运行时需要加载到内存中
      多个程序共享一个dll 
      程序包里必须包含所需的dll  
      dll可以独立于程序进行更新，比如某个算法库更新，编译出新的dll替换到现有的程序包里就好
 -dll的优势：
      节省空间，可执行文件exe的大小更小
      因为多个程序可以共享一个dll，所以可以节省运行时的空间
      便于更新，某个库可以进行单独的更新不需要整个程序的更新

