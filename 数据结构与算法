=====数组====
定义：数组是存放在连续内存空间上，相同数据类型的集合，可以通过下标访问数组
因此：
-数组下标从0开始
-数组的内存空间地址是连续的
-已知下标，访问数组的时候时间复杂度为O(1)
-增删元素的时候要移动其他元素的地址，除非是在末尾添加删除, 时间复杂大约为O(n)

c++中vector和array的区别？
-vector的底层实现是array，严格来说vector的底层是现实array
-一般来说数组元素不能删除只能覆盖
-vector可以通过remove和erase删除
    remove不会真正删除元素或减小容器的大小，返回移动了结束迭代器，容器的size、capacity和end都不变
    erase是真正删除并减小容器大小，返回删除元素后面的的一个元素迭代器，size、end都会变，capacity可能


=====链表====
定义：通过指针串联在一起的线性结构，每个节点都由数据和指针构成
链表存储方式：链表是根据指针链接，内存中的存储不是连续的
链表类型：
-单链表
-双链表
-循环链表

定义一个链表节点示例：
struct ListNode{
    int val;//节点上存储的元素
    ListNode*next; //指向下一个节点的指针
    ListNode(int x):val(x),next(nullptr){} //节点的构造函数
};
-链表查询的时间复杂度为O(n),增删的时间复杂夫为O(1)


=====哈希表====
定义：哈希表是根据关键码的值直接进行访问的数据结构，一般用于快速判断元素是否出现在集合里
实现方式：数组，set(集合),map（映射)
1.set:   数值不可修改
-std::set:红黑树实现，有序，数值不可重复，不可改数值，查询效率logn，增删效率logn
-std::multiset:红黑树实现，有序，数值可重复，不可修改数值，查询效率logn，增删效率logn
-std::unordered_set：哈希表，无序，不可重复，不可更改，查询、增删效率O(1)
2.map: 键值对
-std::map：红黑树实现，key有序，key不可重复，key不可修改，查询、增删效率logn
-std::multimap：红黑树实现，key有序，key可重复，key不可修改，查询、增删的效率logn
-std::unordered_map：哈希表，key无序，不可重复不可修改，查询、增删效率O(1)

KMP算法：
是什么？什么作用？：kmp算法又三个学者发明的由他们的名字首字母命名而来
                  kmp算法主要用于字符串的匹配，用到了前缀表，先计算前缀表next数组，再根据这个前缀表进行回退字符串的匹配
如何计算前缀表：前缀表就是next数组，用于就模式串与主串不匹配的时候，模式串应该回退到哪里开始重新匹配


string 的erase函数的使用：
string& erase(size_type index = 0, size_type count = npos);



