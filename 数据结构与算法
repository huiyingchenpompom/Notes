=====数组====
定义：数组是存放在连续内存空间上，相同数据类型的集合，可以通过下标访问数组
因此：
-数组下标从0开始
-数组的内存空间地址是连续的
-增删元素的时候要移动其他元素的地址，除非是在末尾添加删除

c++中vector和array的区别？
-vector的底层实现是array，严格来说vector的底层是现实array
-一般来说数组元素不能删除只能覆盖
-vector可以通过remove和erase删除
    remove不会真正删除元素或减小容器的大小，只是移动了结束迭代器；
    erase是真正删除并减小容器大小，返回删除元素后面的的一个元素迭代器

