为什么用qt？
-首先qt是跨平台的，同套代码在windows、linux、mac、ios等平台都能运行
-qt是开源的
-qui界面组件比较全面，可以编写不同的风格

qt的核心机制？
-元对象系统：QT的元对象系统提供了用于内部对象通讯的信号槽机制
-属性系统：绑定到QObject派生类的命名特性，通过元对象系统进行属性的访问无需使用类的成员函数
-信号与槽：信号与槽是用于对象之间的通讯。

元对象系统：
  1.继承自QObject类 
  2.声明类的时候将Q_OBJECT宏置于类的开头位置并且是private，编译的时候会查找这个宏，
  3.元对象编译器moc，moc会为每个QObject子类提供元对象的特性，编译成moc_*.cpp文件
  4.运行的时候，通过这些代码实现元对象的功能
--元对象特性：
    信号与槽可以发送信号个相应的槽；
    获取运行时的信息，比如动态获取类名QObject *obj = getObjectFromSomewhere();MyClass *myObj = qobject_cast<MyClass*>(obj);  ；
    动态的设置属性系统，通过QT_PROPERTY来进行定义
    可以动态的创建对象，比如运行时加载qt的部件

属性系统：
  基于元对象系统，qobject，声明的类继承自qobject，Q_OBJECT的宏，跟着可以加Q_PROPERTY的宏
  实现setproperty，obj->setProperty("customProperty", 42);，并在需要获取数据的时候QVariant value = obj->property("customProperty");

信号与槽：
  Qt的元对象系统使得信号与槽的机制得以实现。
  qt中使用信号与槽替代了回调机制
  -信号signal会在特定的事件时候被发出，这个信号可以是qt预定义的比如clicked点击事件，或者自定义信号自定义的信号需要进行一个信号的注册register
  -槽slot是在相应特定信号时候会被调用的方法。
--信号与槽机制的原理:
  信号与槽的具体流程：
  1.moc查找头文件中的signals，slots，标记信号与槽
  2.将信号与槽的信息存储到类静态变量staticMetaObject中，并按声明的顺序进行存放，建立索引
  3.当发现有connect连接的时候，将信号槽的索引信息放到一个map中，进行一个配对
  4.调用emit的时候，调用信号函数，传递发送信号的对象指针，元对象指针，信号索引，参数列表到active函数,active函数是一个信号发射的函数
  5.通过active函数找到在map中与信号对应的槽索引
  6.根据索引找到槽函数，执行槽函数


clicked事件和pressed事件的区别？
-pressed信号：当鼠标按下按钮的时候立即就会触发
-clicked信号：当鼠标按下并释放的完整的点击动作后触发

qt的信号槽机制的优势和不足？
优点：
  -松耦合：信号发送者和接收者之间无直接的依赖关系；一个信号可以连接多个槽函数无需修改发送者
  -类型安全：编译时会进行类型检查
  -支持多线程：可以确保不同连接的时候线程间的安全通信，多线程的时候通过队列连接处理线程的同步
  -自动管理内存：对象被销毁的时候会自动清理连接，这也是元对象系统的一个优点，会自动管理内存
缺点：
  -增加性能开销：比直接调用函数慢，运行的时候需要查找方法索引，发送active找到槽函数指针等。
  -增加构建的复杂度：构建的时候需要额外进行moc的构建
  -会有一些限制：必须继承自QObject，并且包含Q_OBJECT宏，

回调机制？
回调机制允许一段代码在特定的事件发生的时候回调另一段的代码，可以实现异步编程、事件驱动架构、控制反转
回调机制的核心：将函数作为参数传递给其他函数，以便在适当的时机调用
回调的类型：
  1.同步回调：主函数执行的过程中立即调用
  2.异步回调：在主函数执行完成后的某个时间点进行调用
qt的信号槽本质就是一种回调机制，先建立信号槽的连接，发送信号消息的时候在激活调用槽函数
线程的异步操作也可以实现回调，比如等待某个线程任务、获取到finished消息，以后可以对数据进行一个后处理

回调机制的优缺点？
优点：
  -调用者与被调用者是解耦
  -可以动态的改变响应函数，比较灵活
  -适用于异步操作、不会阻塞主线程
  -可以基于回调实现发布订阅模式
缺点：
  -回调的嵌套比较复杂的时候，代码比较难理解，代码的执行会比较难追踪因为有可能是异步的操作
  -调式的时候难以找到上下文


订阅发布系统架构相对于原有的qt信号槽的模式的优势与缺点？
1.订阅发布优点：
  -完全解耦：发布者无需知道有咩有订阅对象，组件之间也不需要直接的依赖关系
  -基于名称的事件系统：使用字符串标识事件，更灵活也增加了可读性
  -动态的订阅管理：集中api管理订阅关系
2.订阅发布的缺点：
  -缺乏类型安全：跟qt的信号槽机制比起来，没有编译期的参数类型检查，但是这样就减少的编译时的开销

信号和槽的本质？
本质就是回调函数。槽函数根据信号或者接收值做出相应的操作

qt中信号和槽的原理？
-首先信号和槽是qt的核心机制，主要用于对象间通信
当一个对象的状态发生了改变，会发出一个信号signal，任何连接到这个信号的槽函数slot都会被调用。类似于设计模式中的观察者模式，类似于发布订阅模式
信号和槽机制：
-通过moc生成代码:
  qt使用moc的工具解析元对象信息，也就是moc读取Q_OBJECT宏，生成相应的元对象代码，包括信号和槽的实现，也就实现了信号和槽的连接
-类型安全:
 信号和槽是类型安全的，如果槽函数参数的类型与信号的参数类型不匹配，编译会报错。

qt的元对象？
元对象指的是QObject类，所有使用元对象的类都会直接或者间接继承自QObject
元对象(QMetaObject)是静态的，每个QObject派生类都有一个静态的元对象实例。
元对象(QMetaObject)在程序启动时创建，程序结束时销毁。

什么是moc？
moc是一个代码生成器工具
  预处理阶段，moc会在编译前处理Q_OBJECT宏头文件，为每个使用Q_OBJECT宏的类生成moc_*.cpp文件
  文件包括相应的元对象结构，包括信号和槽的索引，支持信号发射和槽的调用，基于这个文件就实现了信号和槽的机制


有哪些connect函数？
connect(button, &QPushButton::clicked, this, &MyClass::handleClick);
connect(button, SIGNAL(clicked()), this, SLOT(handleClick()));
connect(button, &QPushButton::clicked, [=]() { qDebug() << "Button clicked!"; });


qt的样式表(style sheet)?
-qt的样式表是用css的语法定制qt的外观。可以不修改代码就改变样式


如何实现自定义绘制？
Qt中自定义绘制主要通过重写paintEvent()方法并使用QPainter类来实现

举例常用的控件
QPushButton、QLabel、QLineEdit、QGroupBox 、QTabWidget、QComboBox
QListWidget、QTableWidget、QTreeWidget、QGraphicsView

qt的事件循环
程序启动以后QEventLoop就开始一直循环
通过重写eventFilter过滤鼠标事件、键盘事件、绘制事件、定时器事件、或者是自定义事件

Qt中的常见事件类型包括：
鼠标事件：QMouseEvent (点击、移动、滚轮)
键盘事件：QKeyEvent (按键按下、释放)
绘制事件：QPaintEvent (窗口需要重绘)
调整大小事件：QResizeEvent (窗口大小变化)
定时器事件：QTimerEvent (定时器触发)
焦点事件：QFocusEvent (获取或失去焦点)
自定义事件：自定义QEvent子类


qt里如何自定义事件
-注册自定义事件类型
const QEvent::Type MyCustomEventType = static_cast<QEvent::Type>(QEvent::registerEventType());
-创建自定义事件类
-发送自定义事件类
-处理自定义事件

qt的连接方式，connect的第五个参数？
默认值为Qt::AutoConnection，自动选择信号槽连接的方式，如果信号和槽在同一个线程表示为直接连接，不同线程表示为队列连接
直接连接Qt::DirectConnection，信号发出时自动执行槽函数，槽函数在信号发出的线程中执行，立即响应
Qt::QueuedConnection,信号发出以后槽函数调用被放入线程事件队列，槽函数在接收者所属线程的事件循环中执行，适用于跨线程操作，比如ui线程与工作线程的交互
Qt::BlockingQueuedConnection：队列连接，信号发出后线程会阻塞知道槽函数处理完毕
Qt::UniqueConnection：确保信号和槽不会被重复连接
Qt::SingleShotConnection：信号触发一次以后自动断开连接，适用于一次性操作


qt的自绘控件？

qt实现表格？

qt实现绘制线段？

qt的docker？

鼠标事件了解哪些？

实现一个绘图功能？如何实现具体的过程？

qt的model view？架构？

序列化和反序列化？

Qt 的事件过滤器

为什么 new QWidget 不需要 delete

信号与槽的多种用法

事件与信号的区别

详解Qt中的内存管理机制





