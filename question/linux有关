1.linux的虚拟环境搭建？
用VMware创建Ubuntu桌面版的虚拟机
-安装VMware Workstation Player
-下载Ubuntu桌面版镜像文件iso文件
-安装Ubuntu,创建虚拟机，再进行初始化的设置

2.linux基本命令基本操作
pwd  #显示当前工作的目录

ls    #列出当前的目录内容
ls  -l  #详细列出文件
ls -lh  #以人类可读格式显示文件大小
ls -a   #显示隐藏文件(以.开头文件)

cd /路径 #切换到指定目录
cd ..    #返回上一级
cd ~     #返回主目录
cd -     #返回上一个访问点

touch 文件名  #创建空文件或更新文件时间戳
mkdir 目录名  #创建新目录
mkdir -p 目录/子目录  #创建多级目录
rm 文件名     #删除文件
rm -r 目录名  #递归删除目录及其内容
rm -f 文件名  #强制删除文件
cp 源文件 目标文件  #复制文件
cp -r 源目录 目标目录  #递归复制目录
mv 源文件 目标文件  #移动文件或重命名

cat 文件名  #显示文件全部内容
less 文件名 #分页显示文件内容
head 文件名 #显示文件开头前10行
tail 文件名 #显示文件末尾10行
tail -f 文件名  #实时查看文件更新

ping 地址  #测试网络连接
ssh 用户名@主机  #远程连接到服务器

对于Debian/Ubuntu系统
apt update             # 更新可用软件包列表
apt upgrade            # 升级所有已安装软件包
apt install 软件包     # 安装软件包
apt remove 软件包      # 移除软件包
apt search 关键词      # 搜索软件包


3.项目中涉及到linux的使用？
有一个用c++写的算法需要编译成python可以调用。且多个环境下都能的调用的包
于是需要不同环境进行编译，windows环境下编译成dll，linux环境编译成.os
c++编译成python可以调用的包主要使用到swig、和pybind11工具
windows环境下编写.bat脚本文件，脚本要写：
    -swig工具： 先写.i文件，设置环境变量(包括编译环境、swig输入输出文件路径)，swig根据.i文件生成绑定代码
                设置好连接库的路径（比如Python、Levmar和Eigen库路径）
                windows环境下-->用（visaul studio编译器）cl.exe将c++模块编译为python扩展模块.pyd文件
                linux环境下-->用g++  将c++模块编译为.so文件
    -pybind11于swig区别在于，pybind11需要自己写绑定文件，swig是根据.i文件自动生成的绑定文件

linux下设置sh文件的执行权限：chmod +x build_animation.sh
linux下执行脚本: ./build_animation.sh

linux创建文本编辑器：
  -打开终端，按Ctrl+Alt+T
  -输入创建文件的命令：nano myscript.sh
  -编辑文件
  -Ctrl+o保存文件
  -Ctrl+x退出编辑器

在linux环境下的c++开发：
使用VS Code等编辑器编写代码
g++或者clang进行编译
cmake进行构建
gdb lldb进行调试
其他集成开发环境(IDE)：
Qt Creator，跨平台




