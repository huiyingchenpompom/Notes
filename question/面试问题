假设做矩阵乘以矩阵的优化，要怎么做？除了算法层面的优化，调用库，除了多线程 还有什么方法？
-我做过的优化计算蒙皮顶点的时候有研究过这个问题。
主要是几个方面考虑：
-
-算法层面的优化：
    1.循环重排序：通过调整循环顺序提高缓存命中率
    优化前：
    for(int i = 0; i < n; i++)
      for(int j = 0; j < n; j++)
        for(int k = 0; k < n; k++)
            C[i][j] += A[i][k] * B[k][j];
     优化后：
    for(int i = 0; i < n; i++)
      for(int k = 0; k < n; k++)
        for(int j = 0; j < n; j++)
            C[i][j] += A[i][k] * B[k][j];
    cpu加载元素的时候会加载附近的数据到缓存行，按行访问的话后续的访问可能已经命中了加载的缓存行，按列访问会导致缓存每次都未命中，触发昂贵的内存读取
    因为在C/C++中，二维数组采用行主序(row-major)存储，A[0][0], A[0][1], A[0][2]... 在内存中是连续的
    按行获取数据将提高缓存的命中率
    
    2.分块：针对多级缓存的分块计算。
    处理器通常有多级缓存，L1缓存，极快容量小  L2缓存速度快 容量中等 L3容量较大但速度比较慢
    将大矩阵的乘法拆分为多个小块的乘法，以确保：每个计算块能完全装入特定级别的缓存，最大化数据重用
      块大小 ≈ sqrt(L1缓存大小 / (3 * 数据类型大小)) 
      例如，对于32KB的L1缓存和双精度(8字节)矩阵：块大小 ≈ sqrt(32KB / (3 * 8)) ≈ sqrt(1365) ≈ 36，通常取为32或64以便于内存对齐

-缓存优化
    优化矩阵的存储布局：选择行优先或列优先存储
    内存对齐：确保数据对齐到缓存线边界
    预取指令：提前加载即将访问的数据
-SIMD向量化：
    SIMD是单指令多数据并行计算技术
    使用指令集：AVX/AVX2处理多个数据
    编译优化：使用编译器自动向量化指示符
-多线程并行化：
    数据划分：行划分、块划分或动态负载均衡
    细粒度并行：降低同步开销，提高并行度

    使用，Eigen库
    GPU加速：使用cuda、opengl等
    使用专用硬件：TPU/FPGA实现

Eigen库是如何加速矩阵乘法计算的？
-表达式模板技术，表达式不会立即计算而是构建表达式树
        避免临时创建对象，减少内存分配和复制 
        融合了多个操作，比如D=A*B+C只需要遍历一次
-多级分块算法
        根据矩阵大小、缓存大小自动调整分块的参数，
        针对L1 L2 L3缓存进行多级分块
-SIMD向量化
        大量使用SIMD指令实现并行计算
        指令集自适应
        会根据不同的数据类型优化向量长度
        区分对齐内存和非对齐内存的访问
-特殊矩阵优化
        稀疏矩阵：使用CSR、CSC存储格式和专用乘法算法
        三角矩阵：利用结构特性减少计算
        对称矩阵：就计算必要部分
-编译时的优化：
        利用c++的模板元编程实现编译时的优化template<typename Scalar, int M, int N>
-多线程并行化：
        omp  openmp并行计算，多线程优化
用cuda库
-共享内存优化矩阵乘法
        减少全局内存访问
        增加数据重用
        提高内存带宽的利用率
-合并内存访问，动态并行


主要从以下几个方面回答思考：
内存访问模式优化：减少缓存未命中->遍历的时候尽量按行遍历；对数据分块计算保证内存对齐
并行计算：充分利用多核架构->openmp并行计算
指令级优化：使用SIMD/向量指令 ->simd指令实现并行计算，提供了只允许内存对齐的指令操作，性能较好
算法改进：采用Strassen等高级算法


// C++17标准方式 内存对齐
float* aligned_memory = static_cast<float*>(
    std::aligned_alloc(32, 100 * sizeof(float))
);

矩阵乘法的优化，考虑当前cpu的架构L1 cache  L2 cache进行优化
缓存优化
    优化矩阵的存储布局：选择行优先或列优先存储
        cpu加载元素的时候会加载附近的数据到缓存行，按行访问的话后续的访问可能已经命中了加载的缓存行，按列访问会导致缓存每次都未命中，触发昂贵的内存读取
        因为在C/C++中，二维数组采用行主序(row-major)存储，A[0][0], A[0][1], A[0][2]... 在内存中是连续的
        按行获取数据将提高缓存的命中率

    内存对齐：确保数据对齐到缓存线边界
        处理器通常有多级缓存，L1缓存，极快容量小  L2缓存速度快 容量中等 L3容量较大但速度比较慢
        将大矩阵的乘法拆分为多个小块的乘法，以确保：每个计算块能完全装入特定级别的缓存，最大化数据重用
          块大小 ≈ sqrt(L1缓存大小 / (3 * 数据类型大小)) 
          例如，对于32KB的L1缓存和双精度(8字节)矩阵：块大小 ≈ sqrt(32KB / (3 * 8)) ≈ sqrt(1365) ≈ 36，通常取为32或64以便于内存对齐
        L1_CACHE_SIZE=32kb
        L2_CACHE_SIZE=256KB
        L1分块: L1_BLOCK_SIZE ≈ sqrt(L1_CACHE_SIZE / (3 * sizeof(元素类型)))
        L2分块: L2_BLOCK_SIZE ≈ sqrt(L2_CACHE_SIZE / (3 * sizeof(元素类型)))
            
    预取指令：提前加载即将访问的数据，
        利用c++的模板元编程实现编译时的优化template<typename Scalar, int M, int N>
        
如何计算运行时候花费的cpu时钟数？
-标准函数clock(),可以获取cpu时间(毫秒级别)
-windows系统  x86 RDTSC指令,可以获得时间周期数
    uint64_t start_cycles = rdtsc();
    double cpu_freq_ghz = 3.5;
    double time_ns = (cycles / cpu_freq_ghz);
-linux系统，使用RDTSC指令，uint64_t start= rdtsc();

但是现代cpu是支持动态频率的调整，周期数并不能精确反映执行时间。

为什么用float不用double？
-float单精度浮点数，32位 4字节
-double双精度浮点数，64位 8字节
float内存占用减半，相同的内存空间可以容纳两倍的float元素
大多数gpu对float有优化，性能会高于double
在大多数对于计算性能要求高，数据量大计算密集的场景下float的精度足够了
对于计算精度要求高的情况下科学计算，需要用到double

描述浮点数的精度问题，以及如何避免精度丢失？
问题：
-浮点数的精度问题：十进制的小数转为二进制的时候无法精确表示很多时候需要舍入，比如十进制的0.1 二进制是无限循环的
-会导致小浮点数加减法有误差的问题，很多情况比如相近的数相减，大小数相加
精度丢失策略：
-避免浮点数的比较，比较的时候: fabs(a-b)<1e-9  小于某精度表示相等
-用更小的单位表示，用分表示钱最大限度的减少小数，并且尽量用整数表示，比如用毫秒表示时间，
-浮点数，累加的时候增加补偿项，用一个数来记录每次的舍入误差，把这个舍入误差给加上
-用高精度的库函数，cmath  
-

十进制小数转二进制的过程
0.1 × 2 = 0.2   (取整数部分0)
0.2 × 2 = 0.4   (取整数部分0)
0.4 × 2 = 0.8   (取整数部分0)
0.8 × 2 = 1.6   (取整数部分1)
0.6 × 2 = 1.2   (取整数部分1)
0.2 × 2 = 0.4   (取整数部分0)


并行化计算（OpenMP、CUDA）
-OpenMP：
    使用指令式并行编程，使用#pragma指令注释代码
    omp线程间共享内存空间
    #pragma omp parallel for
    for (int i = 0; i < size; i++) {
        array[i] = heavy_computation(array[i]);
    }
    当代码是简单的多任务并行的时候用omp
-tbb：
    基于任务的并行模型，不直接管理线程，而是将工作分为任务
    使用模板库，基于任务的并行，提供并行的容器和算法
    涉及到比较复杂的并行，是不同复杂度的并行任务
-cuda是基于英伟达的gpu并行计算：SIMT (单指令多线程) 执行模型
    线程块内的线程共享内存
    所有线程可以访问全局内存
    内存合并访问
    
    
高性能矩阵运算（BLAS库或Eigen库）
-调整循环的顺序：
    按照内存顺序循环；
    按行访问而不是按列访问。
    按行访问的话后续的访问可能已经命中了加载的缓存行，按列访问会导致缓存每次都未命中，触发昂贵的内存读取
-分块矩阵运算：
    将大矩阵分解为小块：
    处理器通常有多级缓存，L1缓存，极快容量小32kb  L2缓存速度快256kb 容量中等 L3容量较大但速度比较慢
     块大小 ≈ sqrt(L1缓存大小 / (3 * 数据类型大小)) 
-尽量内存对齐，可以提高效率
-SIMD单指令多数据实现数据并行
-Eigen利用模板元编程实现编译期向量化
-自适应优化的算法：
    根据矩阵尺寸和缓存大小来调整分块大小
    根据核心数矩阵大小 设置线程数量

块大小 ≈ sqrt(L1缓存大小 / (3 * 数据类型大小))   分块计算的时候为什么是3*数据大小
计算 C = A × B 的分块矩阵乘法中，计算一个 b×b 大小的 C 子块时，我们需要同时访问：
A 矩阵的一个 b×b 子块（需要 b² 个元素）
B 矩阵的一个 b×b 子块（需要 b² 个元素）
C 矩阵的输出 b×b 子块（需要 b² 个元素）
因此，要在缓存中维持的总元素数 = 3 × b²

为什么要内存对齐
cpu一次读取一个字，32位读取4字节，64位读取8字节
当数据跨越字边界的时候，cpu需要进行多次内存访问还要合并结果
--内存对齐的时候只需要一次的访问内存
--内存不对齐 ：两次内存访问+合并

// C++17标准库对齐分配器
std::vector<float, std::aligned_alloc<float, 32>> aligned_vector;

内存不对其还会导致内存占用增加，频繁的内存不对其会导致内存碎片过多内存泄漏的问题

 C++17新特性：
-constexpr if  编译期选择代码路径，可以简化模板代码
if constexpr (std::is_integral_v<T>) {
        return t * 2;  // 整数类型时的操作
    } else if constexpr (std::is_floating_point_v<T>) {
        return t * 2.5; // 浮点类型时的操作
    } else {
        return t;       // 其他类型
    }
-不需要在创建模板类示例的时候明确指定模板参数,会自动推导类型
比如
//C++17之前 std::vector<int> v1{1, 2, 3};
//C++17写法 std::vector v2{1, 2, 3};
-std::any 可以持有任何类型的值，std::any_cast<
-std::string_view，提供字符串的引用，避免不必要的拷贝
-std::filesystem 
-优化了unordered_map ,创建的时候用 emplace back  而不是insert
c++20：
-允许对模板参数进行编译时约束，


什么是编译期向量化
-定义：编译期的向量化是一种在编译时将操作转换为simd单指令多数据的向量操作。
        编译期向量化通过模板元变成在代码生成的阶段就是先了优化
可以实现一次性计算，避免临时创建对象导致的开销
模板实现了静态多态，没有虚函数的开销。Eigen库是有用到编译期向量化和模板优化


运行时多态有虚函数的开销？时间复杂度是？
大多数情况下虚函数的开销跟直接调用开销差别并不明显，但是如果循环频繁的调用虚函数，或者对延迟非常敏感的系统 还是还是会有差别
-虚函数的基本开销：
    内存开销：
        虚函数表指针，每个包含书函数的类都有一个指向虚函数表的指针
        虚函数表，每个有虚函数的类都有一个虚函数表，包含指向该类所有的虚函数实现的指针
    运行时的开销：
        首先虚函数的调用：
            1.从对象加载虚指针  2.从虚函数表加载函数指针   3.调用函数指针
        非虚函数的调用：直接调用
        跟非虚函数相比：
            额外增加了内存调用，而且虚函数表缓存未命中的情况下还会有更多的cpu时钟周期的延迟 大概100-300个
虚函数的时间复杂度是O(1)
如何减少虚函数的开销：
    用模板实现编译时的静态多态
    一定要用的情况下，调用虚函数之前先变换类型 dynamic_cast
// 优化前: 循环中的虚函数调用
for (int i = 0; i < 1000000; i++) {
    obj->virtualMethod();  // 每次迭代都有虚调用开销
}

// 优化后: 循环前确定类型
auto method = [obj]() {
    if (auto derived = dynamic_cast<Derived*>(obj))
        return [derived]() { derived->concreteMethod(); };
    return [obj]() { obj->virtualMethod(); };
}();

for (int i = 0; i < 1000000; i++) {
    method();  // 没有虚调用开销
}
编译器知道确切类型，可能执行devirtualization（去虚拟化）优化
（这里的优化主要在于提前类型转换不需要每次都查询虚指针和虚表
优化说明： lambda函数将运行时多态转换为编译期的多态
        只需要在循环之前查找一次虚表，不需要每次都查找虚指针和虚表
lambda在编译时：
-编译器会生成一个lambda的闭包类型，匿名的类
-确定捕获变量的存储方式
lambda运行时：
-创建闭包对象，执行捕获变量的复制和引用
lambda的存储：
    有捕获的lambda存储在栈上
    临时的没有捕获的存在堆
lambda被当作参数传递的时候存在堆栈中

如何实现一个高效的时间序列处理框架？
-高效的时间序列索引，时间序列框架一般是写多，范围查询为主，需要达到存储的高效、查询高效
-首先需要一个合适的数据存储结构：
    时间序列通常按照列式存储；
    按照时间进行分区
-存储引擎需要支持高效写入和查询：
    通过分级架构，热数据存在内存，其他数据存在ssd
-然后需要注意内存管理：
    要内存对齐，尽量避免内存碎片，减少开销
-用多线程并行查询，不同时间分区并行处理
-参考一些现有的系统设计比如influxDB、KDB+ /q的向量化操作模型
-总之就是从数据结构、存储优化、查询优化、以及内存管理方面的性能有考考虑。


什么是InfluxDB、KDB+？
都是处理时间序列数据

优化查询？
如果是数据库的查询
-优化索引设计
    比如为where条件、join条件都创建合适的索引
-重写查询语句
    比如删除不必要的子查询
-数据库架构进行优化
    比如分布式查询
-还有就是缓存策略角度考虑
    使用应用层的缓存比如 redis
    多级缓存的设计


描述如何优化高频交易系统的延迟。
-首先高频交易对延迟敏感，微秒甚至纳秒的差异会导致交易的成败
cpu处理器的优化：
-CPU使用高时钟频率，这样时钟周期会更短，时间=时钟周期/时钟频率
-避免cpu动态调整频率，不用节能的选项
内存优化：
-使用高速内存，低延迟的专业内存 DDR5
-程序启动的时候进行内存的预分配，避免运行时的内存分配
-减少缓存未命中，以及尽量内存对齐
网络优化：
-使用专用的网络
网络协议的优化
-自定义网络协议，精简网络协议，减少开销
-用tcp的时候，优化tcp的拥塞控制的算法
-用udp代替tcp
算法优化：
-用整数代替浮点运算
-预计常用的函数的计算结果存在表里
-用并行计算等
FPGA加速: 将关键算法实现在FPGA上

实现一个简单的TCP服务器，接收客户端发送的整数，返回其平方值。
1.实现服务器
    创建tcp套接字，设置地址和端口等：server_fd = socket(AF_INET, SOCK_STREAM, 0);
    绑定端口：bind(server_fd, (struct sockaddr *)&address, sizeof(address));
2.启动服务器，进入监听循环
    // 监听连接
    listen(server_fd, 5);
3.循环接收客户端连接
    //创建线程处理客户端的数据
4.从连接读取整数，计算并发送结果给客户端
--最好使用io多路复用+工作线程混合
多线程，每个连接创建独立线程、进程。
poll实现多路复用，使用pollfd结构数组，新的socket连接就存在数组里，使用完了就删除
--I/O多路复用可以实现单个线程管理多个连接，共享内存空间，减少上下文切换的开销

I/O多路复用的瓶颈？
-cpu是单线程串行处理，复杂计算可能会阻塞连接
-网络带宽：总体网络吞吐量仍是共享资源


使用C++实现一个支持并发访问的LRU缓存
-LRU是最近最少使用，淘汰最近最少使用
使用哈希表std::unordered_map，可以提供O(1的时间复杂度的查找)
双向链表：维护数据的访问顺序，最近访问的项放在链头，最久未访问的放在链表尾部

1.多线程如何进行数据同步？
-互斥锁
    互斥锁确保同一时间只有一个线程访问共享资源
-读写锁
    允许多线程读数据，但是写入时需要独占访问
-原子操作
    std::atomic<int> atomic_counter(0);
    int current = atomic_counter.load();
-设置条件变量
    线程间的通信，一个线程完成任务通过条件变量通知另一个线程可以开始下一个任务了
-信号量
    信号量控制访问资源的线程数量。c++20正式支持了信号量，#include <semaphore>


2.mutex从最顶层到操作系统，如何保证互斥
-代码层面 c++标准库 mutex  lock()  unlock()， 调用接口api
-标准库的实现，将api接口翻译为操作系统的原语，通过原子操作或者调用操作系统提供的同步原语
-用户态无法解决竞争问题的时候，需要进行系统调用：
    内核介入：linux通过 futex ，windows通过releaseMutex

内核的互斥逻辑：
    等待队列，调度决策看哪个等待线程获得锁，跟踪锁的状态
    持有锁的线程释放锁时，内核从等待队列选择一个线程唤醒
操作系统最终是依赖处理器提供的原子指令来实现互斥操作
    

3.linux的用户态和内核态的概念？应用程序如何从用户态跑到内核态？
-用户态：
    应用程序受限制的执行环境，无法执行特权指令，比如直接访问物理内存和硬件设备
    可以访问应用程序
-内核态：
    操作系统内核运行的高特权执行环境
    可以直接访问硬件资源，可以执行所有的指令，包括内存管理，进程调度等
用户态切换到内核态的方式：
-系统调用： 这是用户态到内核态的主动切换方式，比如 open(),read(),write(),socket()等、还有socket的send()、recv() 也实现系统调用
-异常：程序执行过程的非法操作，比如除零，内核会执行终止程序
-中断：外部设备或者定时器发出的异步信号，比如键盘输入之类的
-信号处理：内核或其他进程发送的通知

-系统调用的工作机制：
    调用库函数：应用程序调用c库函数
    设置系统调用号：库函数准备系统调用号和参数
    触发陷入指令
    CPU的特权级转换
    执行内核代码
    返回用户态
-用户态切换到内核态的性能考虑：
    每次都要保存和恢复处理器状态
    频繁的系统调用会导致性能下降
    要尽量减少系统调用--使用缓冲（I/O）
-缓冲（I/O）:
    应用程序写入数据到用户空间缓冲区，缓冲区是应用程序和操作系统之间增加的中间层 内存缓冲技术
    写缓冲的时候：当缓冲区满、手动调用关闭文件、或者程序正常退出的时候，才执行实际的write系统调用
    读缓冲的时候：首次读取的时候进行系统调用read(),一次性读取较大的数据块到缓冲区，缓冲数据用完以后再次出发系统调用填充缓冲区
    --c++的iostream库就是提供了缓冲I/O
    --socket的缓冲  就是要尽量减少send和recv的调用，

socket / bind / listen / accept / connect

4.浮点数在内存中是如何表示？
IEEE 754标准，32位
在内存中主要由三个部分组成：
    符号位：1位，表示正负
    指数位：8位，表示指数的偏移值
    尾数：23位，表示二进制小数的精度部分
-由于浮点数的尾数位数有限，浮点数无法表示所有的数，十进制转为二进制的时候小数舍入，也会导致精度的缺失问题。
    也就导致了浮点数不能直接进行比较，加减法会导致误差的问题

5.浮点数比较能==吗？自己实现一个性能比较好的浮点数比较方法？
-浮点数不能直接比较
-假设两个浮点数 a，b   fabs(a-b)<=epsilon  ，精度一般为科学计数比如 1e-3


7.实现这个浮点数方法要多少个cu的时间周期？

假设现在实现了：
bool approximately_equal(float a, float b, float epsilon) {
    return fabs(a - b) <= epsilon;
}
加载参数a,b,epsilon:1时钟周期
计算(a-b):3个周期
取正数fabs(a - b)：1-2个周期
执行比较(<=epsilon)：1个周期
返回结果也需要 大约1个周期
所以总共至少7个周期
如果需要精确计算的话：
在执行函数前后拿到计算机的时钟周期：
-通过rdtsc获取计算机的时间周期，uint64_t start_cycles = rdtsc()

8.c++类型转换有几种？各自应用的场景？
-static_cast
    编译时的类型转换，会进行类型检查
    基本数据类型之间的显式转换
    派生类转为基类
    基类转为派生类
    void*转为具体类型指针
-dynamic_cast
    运行时的安全转换，适用于多态类型，运行时会进行类型识别，
    基类转换为派生类
    一个派生类转为另一个派生类
    转换失败返回nullptr
    一定要实现虚函数
-const_cast
    只针对于const的转换
    移除const属性
    将一个const参数传递给非const的函数
-隐式的类型转换
    数值类型之间自动转换，比如int转为double，非const转为const，派生类到基类的转换


9.c++的virtual是如何实现的？虚函数的调用是时间复杂度多少的算法？
-virtual实现了运行时的多态，主要是通过虚函数表和虚函数指针实现的
-类调用虚函数过程：
    对象拿到虚指针->虚指针找到虚表->虚表中找到虚函数->执行虚函数

时间复杂度为O(1)：
    拿到虚指针、访问虚表、根据固定的内存偏移位置找函数、以及调用函数都是常量的执行时间
但是跟普通函数相比也会有多的开销在于
    一个是虚指针和虚表，会有两次的内存访问
    运行函数有一个跳转的时间
内存访问，也分为命中缓存和未命中缓存，如果命中缓存的话可能就很快，但是未命中缓存的话就会导致需要花费更多的时钟周期
优化方式：
    减少查找虚指针和虚表，比如进行类型转换dynamic_cast 

为什么未命中缓存会有跟多的延迟？

当缓存未命中的时候会重新进行缓存行的填充：
    需要经历的过程：cpu流水线暂停-内存控制器发出读请求-内存将整个缓存行传送到缓存-数据通过缓存层级传递-最终拿到数据
计算机内存的不同结构之间访问速度有很大的差异，L1 cache、L2 cache以及L3cache的读取速度都不同
    
10.会看汇编指令吗？用汇编指令描述虚函数的跳转？
-会简单的汇编指令
举例：
class Base {
public:
    virtual void foo() { /*...*/ }
    virtual void bar() { /*...*/ }
};

class Derived : public Base {
public:
    void foo() override { /*...*/ }
};

void callVirtual(Base* obj) {
    obj->foo();  // 虚函数调用
}
# callVirtual(Base* obj)函数的汇编实现
callVirtual:
    #1 从对象加载vptr,虚指针位于对象内存的开始位置
    mov  rax,[rdi]
    #使用vptr加载vtable    
    mov  rax,[rax]
    #调用函数
    jmp  rax    #跳转到函数地址

c++代码编译的全过程：
-写代码，cpp .h
-预处理：
    处理宏、include指令换为代码实现、处理#ifdef #ifndef
    删除注释
-编译阶段：
    编译器比如通过clang gcc将代码转换为汇编代码
-汇编阶段：
    汇编器将汇编代码转换为机器码，也就是目标文件 .o  .obj文件
-链接阶段：
    将目标文件组合成可执行文件，exe、dll、lib

try-catch机制：
-主要是在编译阶段，编译器生成必要的异常表和控制流代码
-其他：
    链接阶段-合成异常表并链接运行时支持
    运行时-使用生成的表执行实际的异常处理

11.smart point，shared_ptr是线程安全的吗？怎么做到的？
-安全操作：
    引用计数的增加减少是线程安全的，因为引用计数的增加减少是原子操作
    多个线程同时读取同一个对象的不同share_ptr
-不安全的操作：
    对同个share_ptr示例并发写操作是线程不安全的，多线程同时修改肯定是会有问题
    ->这个时候需要设置读写锁，来保证线程的安全，c++20也实现了atomic原子智能指针来保证share_ptr的线程安全
-shared_ptr实现线程安全的机制：
    引用计数通过原子操作来实现，可以保证引用计数增加减少的操作是安全的

什么是原子操作？
原子操作就是在执行过程中不可被中断的操作，要么执行要么不执行
<atomic>库
shared_ptr的引用计数就是原子操作：
    计数增加：当复制share_ptr调用复制构造函数、还有赋值的操作（operator=)
    计数减少：当shared_ptr对象析构、调用reset等


12.socket了解吗？windows下的socket有一条特殊的指令知道吗？  WSAStartup ,把这个进程的TCP协议栈初始化
-WSAStartup是windows socket网络编程必须调用的一个函数，用于初始化winsock库和TCP/IP协议栈
-WSAStartup函数：
    初始化TCP/IP协议栈
    分配资源
    返回系统详细信息
实现过程：
    包含winsock2和ws的库  #include <winsock2.h> #include <ws2tcpip.h>
    WSAStartup有两个参数分别是 请求的版本，和指向wsadata的指针
    调用完以后返回值可以判断是否调用成功
    之后可以进行socket操作的代码

#include <winsock2.h>
#include <ws2tcpip.h>
#include <stdio.h>

#pragma comment(lib, "Ws2_32.lib")

int main() {
    WSADATA wsaData;
    int result;

    // 初始化Winsock
    result = WSAStartup(MAKEWORD(2, 2), &wsaData);
    if (result != 0) {
        printf("WSAStartup failed: %d\n", result);
        return 1;
    }

    printf("Winsock initialized successfully\n");
    
    // Socket操作代码...
    
    // 完成后清理Winsock
    WSACleanup();
    return 0;
}

13.如何用UDP实现一个可靠的传输协议？
-udp是一个无连接、不可靠的传输数据，具有低延迟和低开销的特点
想要实现可靠传输：
1.维护udp的连接状态
2.为每个数据包都分配唯一的序号，接收方在接收到数据包以后发送确认的ack消息
3.实现超时重传，避免丢包。发送方没有收到接收确认的消息就重传这个包
4.进行流量控制避免发送方淹没接收方，实现拥塞控制等调整发送的速率
5.进行数据的校验
实现的过程：
1.协议头的设计
2.发送数据包的函数实现
3.接收数据包的函数实现
现有的可靠UDP协议：QUIC\UDP等

自己实现的udp，跟tcp比起来：
优势：低延迟，可靠性是自定义很灵活
劣势：实现会比较复杂，可靠性比不过tcp


14.stl知道哪些数据结构？
序列容器：
-vector：动态数组，随机访问，插入删除O(n)
-list：链表，两端插入删除O(1)，查找需要遍历
-deque：分段数组，随机访问，插入删除O(1)
关联容器：
-map：红黑树实现的键值对，会自动排序，查找、插入、删除的时间复杂度为logn
-unordered_map：哈希表，无序键值对，平均查找O(1),最坏O(n)
-set：红黑树实现的 唯一有序键， 查找插入删除时间复杂度为logn
-unordered_set：哈希表，唯一键，平均查找O(1),最坏O(n)
容器适配器
-stack：底层容器是deque，先进后出
-queue：底层容器是deque，先进先出
-priority_queue：优先级队列，底层容器是vector，可以实现大顶堆小顶堆
c++17新增：
-string_view：字符序列的只读试图，不需要复制底层数据，零拷贝高效率，不管理内存
类似于const string& 的效果

15.unordered_map有几个参数？与map的区别？
-unordered_map 的参数：键、值是一定有的，还有哈希函数、键比较函数、以及分配器
    哈希函数可以自定义：负责将键转换为整数哈希值，可以决定元素在哈希表中的位置
    键比较函数：用于确定两个键是否相等，因为键有可能是一个比较复杂的数据类，需要自定义键比较函数
    分配器控制容器内存分配和释放的细节。
template<
    class Key,                                     // 键类型
    class T,                                       // 值类型
    class Hash = std::hash<Key>,                   // 哈希函数
    class KeyEqual = std::equal_to<Key>,           // 键相等比较函数
    class Allocator = std::allocator<std::pair<const Key, T>>  // 分配器
> class unordered_map;
map参数：键、值、比较函数、分配器
template<
    class Key,                                     // 键类型 
    class T,                                       // 值类型
    class Compare = std::less<Key>,                // 比较函数
    class Allocator = std::allocator<std::pair<const Key, T>>  // 分配器
> class map;
unordered_map是基于哈希表实现的无序的键值对容器
    插入、删除、查找的时间复杂度为O(1),最坏是O(n)，迭代的时间复杂度是O(1)
unordered_map使用的是链式哈希，当两个键的哈希值相同的情况下，会将元素存在一个桶的链表里，后续查找的时候会需要遍历这个桶。可以将链表存为平衡树，这样时间复杂度为LOgn
        当输入的元素超过阈值的时候会进行重哈希，这时候的时间复杂度也是O(n)， 可以初始化的时候预分配内存
        
map是基于红黑树实现的有序的键值对容器
    插入删除查找时间复杂度Ologn，迭代O(n)
    
16.如何实现unordered_map的下标操作符
-实现operator[]，operator[]的功能：
    查找:计算键哈希值，定位到对应的位置，遍历桶中元素查找匹配值
    插入：如果键不存在，创建并插入新元素到头部
    返回引用：
T&operator[](const Key&key){
    //查找下标
    auto it=this->find(key);
    //没找到的时候就插入新的键值对
    if(it==this->end()){
        auto result=this->insert(std::make_pair(key,T()));
        it=result.first;
    }
    return it->second;
}


 C++17优化：try_emplace：
-c++17引入了try_emplace方法优化了unordered_map
    避免了创建临时对象，避免不必要的移动复制操作，直接在位置上构造

operator[]与at()的比较？
-当键不存在的时候，[]直接插入新元素，at会抛出out_of_range异常
-const对象，[]不可用，at可用

哈希表中的桶bucket：
-桶是存储元素的最基本容器单元，是哈希表内部核心组成部分
-本质是一个数组，可以存储零个、一个或多个元素
-桶进行哈希计算、桶映射、元素存储、冲突处理


17.unordered_map内部是怎么实现的？
-unordered_map内部是一个基于哈希表的数据结构
1.桶数组：存储元素链表的表头
2.节点：存储键值对 以及链接信息的结构
3.哈希函数：计算键的哈希值
4.比较函数：判断键是否相等
5.迭代器进行内存的管理

18.unordered_map有什么性能上的问题？从哪些方面进行优化？
-平均情况下提供O(1)的查找性能
影响性能的问题：
    哈希冲突的问题，当多个不同键映射到同一个桶，可能需要遍历比较键是否相同
    重哈希的开销，当元素数量增长超过阈值，整个哈希表需要重新分配并迁移
    不是连续存储的内存，节点分散在内存中，缓存命中率低
    默认的哈希函数可能不够高效
-优化：
    自定义更高效的哈希函数，减少哈希冲突
    预先分配好足够的空间避免重哈希
    

    
emplace_back函数和insert函数的区别
emplace_back：直接将构造好的参数转发到容器中，在容器内原地构造对象
insert:需要提前构造好完整的对象，然后将其复制或移动到容器中
c++17以后引入：try_emplace,try_emplace会判断键是否存在，不存在的时候才构造对象


19.拉链法性能不好的问题是什么？
拉链法是哈希表中用来解决哈希冲突的问题，广泛应用于unordered_map的实现中
问题：
    缓存不友好：存储的内存位置不连续，链表节点是分散的，会导致缓存缺失问题
    最坏的情况下会导致查找的时间复杂度为O(n)，查找到特定的键需要遍历链表每个元素
    有额外的内存开销存储指针
其他哈希方法:
    开放寻址，线性探索：
        当冲突发生，按顺序查找下一个位置直到找到空槽，这样数据就是连续保存的，但是删除操作查找很麻烦
    在线性哈希的基础上将已有元素往后移动，可以减少最坏情况下的查找
    布谷鸟哈希：
        使用多个哈希函数和表，每个键只出现在其中一个位置，可以提高内存利用率
    用tbb并发访问哈希表，提高查找效率
    跳房子哈希：结合开放寻址和拉链法，每个键都有一个领域，确保键总是在原始哈希的附近
    为特定的静态集合构建无冲突的哈希函数，适合只读的数据集
    

动态链接和静态链接的区别？
静态链接：
    在编译阶段就将依赖库的代码合并到可执行文件中，通常是 .a文件和.lib文件
    静态链接的库更新的时候，整个应用程序都需要重新编译链接
    多个可执行程序的情况下，每个程序都拷贝一个副本，会导致磁盘空间的浪费
    启动速度更快
动态链接：
    可执行文件只包含依赖库的引用，代码只有在程序运行的时候才加载到内存中，通常是.so或.dll文件
    库在更新的时候，只需要替换动态库文件，应用程序无序重新编译
    多个应用程序共享一个库文件
    启动速度更慢
    更灵活

内存对齐？
-数据在内存中的存放位置需要满足对齐的要求，比如4字节的数据放在4的整数倍地址上
-内存对齐的情况下，只需要一次访问固定的内存块
-未对齐的情况下，cpu需要两次访问两个内存块，在做合并的操作，增加了时间延迟
原子操作一般都要求数据对齐
分配的内存不对齐还会导致内存碎片，内存碎片会导致性能下降
c++17提供的内存对齐的内存分配方式：void* aligned_ptr = std::aligned_alloc(16, size);

线程进程协成的区别
-进程是操作系统分配资源的基本单位，拥有独立的内存空间和系统资源
    一个进程无法直接访问其他进程的资源
    上下文切换比较复杂，创建销毁的开销比较大
-线程是进程内的执行单元，是cpu调度基本单位，同进程内的线程共享内存
    拥有独立的栈内存
    上下文切换、创建销毁的开销都更小一些
    线程适用于cpu密集的并行计算
-协程，由应用程序控制调度，比如函数调用
    共享一个线程内的内存
    适用I/O密集，

空类的大小？为什么  描述空基类的优化
-空类大小为1字节：
    类一定会有一个唯一的地址，类声明了不同的对象，都会有不同地址，因此编译器会强制给空类分配1字节的地址
-空基类的优化：
    允许派生类不为空基类的情况下，不分配额外的内存
    a基类，b派生自a，有一个int数据的成员变量，且不为空，b的大小为4字节
    编译器会将派生类第一个数据成员与空基类放在相同的地址偏移量上，因为空基类不需要存储任何实际的数据
    --因此可以通过空基类的优化来实现
        1.内存布局的优化
        2.以及优化内存大小
-但是如果函数里带有虚函数就不是空基类，会有8字节的虚指针大小

禁止某种不需要调用的函数怎么实现编译器就进行检查？你知道哪些实际的场景例子吗
-头文件成员函数=delete ， C++11 引入的特性，后续任何尝试调用改函数的代码都会在编译期报错
class Example {
public:
    // 禁止复制构造函数
    Example(const Example&) = delete;
    
    // 普通成员函数也可以被删除
    void dangerousOperation() = delete;
};
实际应用场景：
    unique_ptr禁止复制 赋值函数和拷贝函数都=delete
    实现一些线程安全的情况，将获取计数器函数=delete
    同样的两个函数，一个是安全版本一个是内部版本，强制只能使用安全版本的函数，安全版本的函数需要一些身份验证之类的
    多线程情况下，禁止非线程安全的接口，只开放比如引用计数增加的接口
    音视频api禁用 声音过大会损坏设备的接口
    --通过代码约束实现编译时的错误检查，会更加的安全可靠
-将函数声明为私有且不实现
-使用静态断言static_assert：
template <typename T>
void process(T value) {
    static_assert(!std::is_pointer<T>::value, 
                 "Pointers not allowed in this function");
    // 指针类型禁用该函数，处理非指针类型...
}
-c++20引入的方法   concept
template<typename  T>
concept NoPointer=!std::is_pointer_v<T>;

template<NonPointer T>
void process(T value) { /* 实现 */ }
-
-用预处理器注释不运行的代码 #if 0  #endif，，但这不管编译器检查


thread的复制构造函数？
std:thread不支持复制构造函数
std::thread 的复制构造函数是被显式删除的
--// 复制构造函数和复制赋值运算符被删除=delete
禁止原因：
1.一个线程是一个唯一的系统资源，不能复制
2.一个线程只能由一个thread对象拥有
3.避免状态混乱
但是虽然线程不能复制但是可以移动所有权，通过move实现
但是如果需要多个地方访问同一线程，可以使用thread的引用或指针，用share_ptr来管理thread


20.如何找到交易成交额topk及对应成交量
-首先收集所有的交易数据
-按成交额排序
-获取前k个最大值及对应的成交量
--数据量比较小的时候，直接用排序算法，快速排序等 时间复杂度大概是nlogn
--数据量比较大的时候用堆存储， 用小顶堆 存储k个元素，小顶堆取数据的时候会将最小的数据淘汰 最终只剩下前k top元素
-数据更大的时候就需要用到分布式计算的框架、或者是用数据库来维护数据


21.mutex头文件，编译选项，加锁放锁函数，锁的机制，如何进入内核睡眠，需要的锁释放后如何唤醒，唤醒后处理器如何调度
-mutex头文件提供了多种互斥原语，实现互斥锁
    mutex  
    time_mutex
    c++17引入了读写锁 shared_mutex
-编译选项：
    -pthread 在gcc clang下链接pthread的库  ，
    -std=c++11设置支持c++及以上的标准线程库
-加锁 lock  解锁 unlock   超时枷锁 
-超时加锁
if (tmtx.try_lock_for(std::chrono::milliseconds(100))) {
    // 在100毫秒内获取到了锁
    tmtx.unlock();
}
-锁的机制：
    用户态通过原子操作直接获取锁，通过原子指令等获取锁
    如果锁被占用就自旋等待来尝试获取锁
    自旋达到阈值了以后。通过系统调用使线程进入睡眠状态  linux futex windows waitforsignalobject
    锁持有者调用unlock以后被唤醒
-如何进入内核睡眠状态：
    线程通过系统调用 futex进入内核态，内核将线程状态从running转为waiting
    线程加入锁的等待队列
    内核调度另一个线程运行
-释放锁的过程
    首先调用unlock，修改锁状态为可用
    如果监测到等待者，调用唤醒函数futex_wake
    内核查到该锁的等待队列，选择一个或多个线程唤醒
    选中的线程状态会从waiting改为ready
    将线程添加到运行队列，等待调度

22.两个线程，如何快速消费资源队列
-使用互斥锁和条件变量，获取pop()操作加锁，避免冲突，两个线程循环获取资源并处理资源
-使用原子操作可以实现无锁的队列，原子操作是不可以中断的
-双缓冲，两个线程两个队列，对资源队列进行分区 减少竞争

23.用程序确定cpu主频的大小和确定cache大小
频率定义：
频率计算：
    RDTSC指令可以获取cpu的时钟周期数
    clock获取实际时间
    频率=时钟周期数/实际运行时间
-linux可以调用操作系统api  获取cpuinfo找到频率hz
-windows也由直接获取频率的api
缓存大小测量：
-不同缓存级别的访问时间差异是很大的，通过这个特性进行预测
-当数据超过某级缓存容量时，访问时间会突然增加  在没有增加之前访问了多少内存进行计算


24.两个线程同时store，考虑cache会出现什么情况
-两个线程会竞争一个缓存行
-导致缓存行的所有权在两个核心之间传递
-最终导致性能下降，延迟增加
解决办法：
    减少共享数据写入
    使用原子操作避免竞争

25.常用的数据结构与算法
数组、链表、栈、队列、树、哈希表、图

规模比较小的情况下sort用插入排序，规模比较大的情况下用快速排序

27.cpu pipeline，cache优化等低延迟开发相关技术？
-设计缓存友好的数据布局和数据访问模式
-利用并行，线程级并行或者指令级并行
-编译优化

流水线中的各个阶段是什么？这些阶段的主要功能是什么？

如何通过并行处理来提高cpu指令吞吐量？

什么是吞吐量和延迟？流水线如何影响它们？

什么是CPU流水线？它的优点和缺点是什么？

cpu pipeline的常见问题

cpu流水线的优化技术

什么是流水线冲突？如何解决结构冲突？

什么是数据冒险（Data Hazard）？如何处理RAW、WAR、WAW数据依赖？

深度流水线可能带来的问题是什么？

如何衡量分支预测的准确性？

什么是控制冒险（Control Hazard）？如何通过分支预测减少影响

超标量架构如何与流水线结合使用？

什么是乱序执行？它如何解决流水线中的数据冲突？

流水线设计如何影响功耗？

流水线和多线程的区别是什么？它们如何协同工作？

linux的常用命令

linux如何查看当前性能

python装饰器？有哪些使用场景

28.TCP/IP网络协议

29.c++17 的特性

30.如何为std::unordered_map定义一个自定义哈希函数？

31.并行算法（C++17）

32.如何使用std::priority_queue实现一个自定义的最小堆？

33.如何高效地去除容器中的重复元素？

34.std::thread与线程同步

35.互斥锁（std::mutex, std::lock_guard）

36.条件变量（std::condition_variable）

37.原子操作（std::atomic）

38.使用std::mutex实现线程安全的单例模式。

39.解释std::future与std::promise的区别，并举例说明。

40.并行STL算法（std::execution）

41.线程池的实现

42.使用C++17的并行STL算法对大规模数据进行排序。

43.Socket编程基础：bind, listen, accept, connect

44.阻塞与非阻塞IO

45.多路复用（select, poll, epoll）

46.高性能网络编程：Reactor、Proactor模式

47.如何使用Linux Socket API实现一个简单的TCP服务器？

48.如何使用select实现多路复用？

49.内存管理：堆、栈、内存泄漏

50.Linux系统调用：fork, exec, pipe, mmap, read, write

51.描述进程与线程的区别，以及它们的应用场景。

52.如何使用Linux的mmap实现文件的内存映射？

53.如何通过fork和exec创建一个子进程，并与其通信？

54.实现一个高效的K线数据合并算法

55.如何使用C++ STL实现Dijkstra算法？

56.描述并实现一个快速的订单撮合引擎。

57.排序算法（快排、归并排序）

58.查找算法（二分查找、哈希）

59.图算法（最短路径、最小生成树

为什么投递量化开发？
-对量化行业有兴趣，我本人也是一直在挑战自我和尝试，想试试看有没有机会加入这个赛道
-本身比较熟悉c++开发、网络编程、stl、linux等，虽然工作领域不同但是技术栈还算比较匹配
-本身是经济学，跟金融也能沾点边，曾经还是学过一些有关课程

为什么量化需要低延迟？
高频交易者的策略通常依赖于极短时间窗口内的市场波动，例如几毫秒甚至微秒级的时间差。
如果延迟太高，可能无法在竞争对手之前执行交易。
在高频交易领域，交易速度是胜负的关键。延迟越低，越能优先获取交易机会。
交易延迟的每一毫秒都可能决定成败。
高频策略如市场制造（Market Making）、套利、趋势跟踪等，都依赖于快速响应市场数据更新和订单执行。

如何实现低延迟？
使用专用硬件，FPGA以硬件级别处理网络数据包，提供微秒级别的延迟
用GPU加速，在需要大规模并行计算时使用GPU
使用低延迟的网卡接口NIC
部署高性能CPU，尽量减少上下文的切换和缓存时效
关键数据存储在内存中，避免频繁的访问磁盘数据或数据库
用udp代替tcp，udp更加轻量级，没有连接和确认的开销，用定制化的网络协议减少数据包大小和处理数据
使用高精度的时钟同步协议来确保时间戳的准确性
减少数据在内存中的拷贝次数，直接从网络接口读取数据进行处理
使用linux内核的实时版本或禁用不必要的内核功能
使用内核旁路技术，例如DPDK和Solarflare OpenOnload，直接访问网卡数据。
使用锁机制最少的并发设计，例如无锁队列
避免线程间的上下文切换
使用高效的数据结构，哈希表，跳表等
优化排序、查找等核心算法，提高运行速度
对传输的数据进行压缩，减少网络负载
实时监控系统延迟，找出瓶颈并优化
减少不必要的计算，将复杂计算离线化，移至关键路径
提前加载可能的交易策略，减少实时计算开销
直接与交易所api交互，避免通过中间或第三方
构建分布式系统，将任务分配到多个节点并行处理
用udp接收实时行情数据
使用UDP发送订单，确保订单路径最短
使用高精度时钟为每个步骤打上时间戳
通过分析延迟日志，优化瓶颈环节

如何 部署高性能CPU，尽量减少上下文的切换和缓存时效

如何关键数据存储在内存中，避免频繁的访问磁盘数据或数据库

如何使用高精度的时钟同步协议来确保时间戳的准确性

如何减少数据在内存中的拷贝次数，直接从网络接口读取数据进行处理










